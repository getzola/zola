[package]
name = "link_checker"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }
errors = { path = "../errors" }
utils = { path = "../utils" }
libs = { path = "../libs" }

[dev-dependencies]
mockito = "1.6"
